[build]
builder = "NIXPACKS"
NIXPACKS_START_CMD = "npx next start -H 0.0.0.0"

[deploy]
startCommand = "npx next start -H 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
